using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TaskRequest
{
    // Start is called before the first frame update
    public string requestKey;
    public Agent requestingAgent;
}
